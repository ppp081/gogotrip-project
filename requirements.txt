# --- Django + API + static ---
Django==4.2.11
djangorestframework==3.16.0
django-cors-headers==4.7.0
whitenoise==6.11.0
python-dotenv==1.1.1

# --- Database ---
psycopg2-binary==2.9.10

# --- ASGI / WebSocket (Daphne + Channels) ---
daphne==4.2.1
channels==4.3.2
channels-redis==4.2.1

# --- LangChain / LangGraph (เรียก LLM ผ่าน API เท่านั้น — ไม่ติดตั้ง torch/CUDA) ---
langchain==1.2.12
langchain-core==1.2.19
langchain-openai==1.1.11
langchain-google-genai==4.2.1
langchain-community==0.4.1
langgraph==1.1.2

# --- LINE Messaging API ---
line-bot-sdk==3.19.1

# --- HTTP & เวลา (ใช้ใน agent / slipok / supabase) ---
httpx==0.28.1
requests==2.32.5
pytz==2025.2
python-dateutil==2.9.0.post0
