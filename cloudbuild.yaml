# Cloud Build: build and push image (same as gcloud builds submit --tag ...)
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - gcr.io/$PROJECT_ID/tripbot-backend
      - .
images:
  - gcr.io/$PROJECT_ID/tripbot-backend
